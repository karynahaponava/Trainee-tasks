# Log Analyzer Alert System

Профессиональная система мониторинга логов мобильных приложений. Система автоматически отслеживает критические ошибки в CSV-файлах и генерирует алерты на основе заданных бизнес-правил.

## Функциональность

Система работает в фоновом режиме, сканирует папку с входящими данными и проверяет их на соответствие правилам:

* **Правило 2.1 (Critical Error Spike):** Обнаруживает более **10 ошибок Fatal** в течение 1 минуты.
* **Правило 2.2 (Bundle Anomaly):** Обнаруживает более **10 ошибок Fatal** для конкретного `bundle_id` за скользящее окно в 1 час.
* **Эффективность:** Читает огромные файлы (Big Data) частями (chunks), не перегружая память.
* **Изоляция:** Полностью упакована в Docker.

## 📂 Структура проекта 

```text
alert_project/
├── data/               # Папка для входящих CSV файлов
├── src/                # Исходный код
│   ├── logger.py       # Настройка логирования
│   ├── processor.py    # Логика чтения файлов
│   └── rules.py        # Бизнес-правила (Rules)
├── tests/              # Автотесты (pytest)
├── .env                # Настройки конфигурации
├── docker-compose.yml  # Запуск контейнера
├── Dockerfile          # Сборка образа
├── main.py             # Точка входа
└── requirements.txt    # Зависимости
```

## Установка и Запуск

### Вариант 1: Запуск через Docker (Рекомендуемый)
Убедитесь, что у вас установлены Docker и Docker Compose.

Настройка окружения: Создайте файл .env:
```bash
INPUT_DIR=./data
PROCESSED_DIR=./processed
LOG_LEVEL=INFO
POLL_INTERVAL=3
```
Запуск системы: Выполните команду в терминале:
```bash
docker-compose up --build
```

#### Использование:

Положите любой .csv файл с логами в папку data/.
Система автоматически подхватит файл, проверит его и выведет алерты в консоль.
После обработки файл будет переименован в .processed.

### Вариант 2: Локальный запуск (для разработки)
Установите зависимости:
```bash
pip install -r requirements.txt
```

Запустите скрипт:
```bash
python main.py
```

## Тестирование
Проект покрыт unit-тестами. Для запуска проверки выполните:
```bash
pytest tests/
```

## Формат данных
Система ожидает CSV-файлы (без заголовков), соответствующие следующей структуре колонок: error_code, error_message, severity, log_location, mode, model, graphics, session_id, sdkv, test_mode, flow_id, flow_type, sdk_date, publisher_id, game_id, bundle_id, appv, language, os, adv_id, gdpr, ccpa, country_code, date (Unix Timestamp).

## Технологии
Python 3.11
Pandas (Data Processing)
Docker & Docker Compose
Pytest (Testing)